# LongStitch 交付切片

当前阶段：E1–E2 已完成并分别提交；S4 仍待外部条件。一次只推进一个切片；每片完成后记录新证据并独立 commit，再推进下一片。用户已授权逐片提交；保持 main，不改写历史。原始 samples、生成的私人截图与环境配置不提交、不部署。

## 产品约束

- 选择同一页面的连续手机截图，自动去重叠并导出完整宽度长图。
- 匹配遮罩与输出裁剪分开：上下常驻栏、浮动工具栏、右侧滚动条不能导致整条正文被粗暴删除。可用相邻图真实像素覆盖遮挡；没有可用像素时保留并提示。
- 图片本地处理，无图片上传、云存储、生成补图。低置信度明确要求校正，不悄悄堆叠或丢内容。
- 原图清晰输出，手动位移/接缝校正、排序、固定区修正与基本隐私遮挡构成完整用户路径。
- 广告后续接入；编辑页独立文档且不加载广告。当前不创建广告账户、不伪造发布者 ID。

## 切片与验收

| 切片 | 用户结果 / 边界 | 依赖 | 状态 | 验收证据 |
| --- | --- | --- | --- | --- |
| S0 调研与交付契约 | 竞品、部署/广告限制及可执行切片；隔离私人样本 | 无 | Done | [调研报告](research/2026-09-09-product-research.md)，合成输入/竞品导出已核验尺寸 |
| S1 本地拼接核心 | 对齐相邻截图、拒绝歧义/无重叠、整宽接缝与真实遮挡替换；原始样本可分析和生成本地结果 | S0 | Done | 10 个核心回归通过；真实样本 1179×11444，全宽 5 接缝目视检查；见执行记录 |
| S2 完整网页编辑路径 | 导入/拖放/粘贴、排序/删除、后台分析/取消、全图与接缝预览、手动校正、遮挡编辑、PNG/JPEG 保存/分享 | S1 | Done | 浏览器真实样本 PNG；已知答案逐像素相等；遮挡/JPEG/手动恢复通过；见记录 |
| S3 双语站点与可部署交付 | 中英文指南/页面、内存与导出限制、广告隔离、Workers Static Assets / Pages 构建、操作文档 | S2 | Done | verify / audit / dry-run 通过；14 页；Cloudflare 本地服务与 390px 视口下载验收；见记录 |
| S4 真实发布与广告开通 | 指定 Cloudflare 账户/域名上的生产发布、真机与大陆网络检查；后续 AdSense 申请与广告上线 | S3 | Deferred / external | 需发布目标、实际运营主体/联系方式、广告审核及真实设备；不以本地模拟代替 |

| E1 遮挡范围可视化 | 在原截图上框选、拖动、缩放、删除与撤销范围，保留真实覆盖替换语义 | S3 | Done | 15 测试及 verify 通过；桌面框选/移动/缩放/撤销，390px 窄屏，重置导出逐像素一致 |
| E2 长图首尾裁剪 | 直观调整最终长图的顶部/底部边界，实时标出删除区；预览与导出一致，保护隐私遮挡位置 | E1 | Done | 19 测试与 verify 通过；模拟触摸首尾裁剪，原图/缩小导出像素核验，遮挡跟随内容 |

S4 是后续外部门槛，不阻塞 S1–S3。S3 的 Done 只表示本地可部署版本完成，不表示真机、生产或广告已经通过。

## 执行记录

### S0 · 2026-09-09

- 调研完成：原生竞品功能来自官方说明；Upliftorch 走完合成图导入和下载，只做直接排列。
- 明确用户新增要求：固定元素与滚动条的匹配遮罩不能变成输出边缘裁剪。
- 已核实当前仓库无历史提交，仅调研与 6 张私有 samples。
- 建立忽略项，避免将私有样本、测试输出和环境变量提交/公开。
- 验证：调研 5 个本地资源链接存在；合成输入 400×640，实际竞品输出 400×1920。

### S1 · 2026-09-09

- 实现 TypeScript 原图行描述、多锚点候选、重叠区复核、整数位移、整宽接缝、重复/歧义检查。浏览器与本地脚本共用同一核心，不调用外部服务。
- 滚动条识别只产生局部遮罩，替换前核验邻近内容对应，无法恢复像素保留原样并计数。完全重复检查覆盖全部像素，右侧正文差异不会被忽略。
- Red：最初缺少模块；初实现暴露重复列表误匹配、浮动底栏污染评分。加入内容复核、有限异常块剔除后 `npm test` 10/10 通过，包括已知原图像素逐点相等、固定栏、浮层、滚动条、无重叠、周期歧义、宽度不一致与不可恢复遮挡。
- `npm run samples`：5 个位移 1497 / 2077 / 1939 / 1798 / 1577；输出 1179×11444；6118 个遮挡像素从其他原图恢复，标记遮罩内无法恢复 0。分析约 232ms，为本机测量，不是手机性能承诺。
- 独立 Python 原始 RGB 重叠检查：第 1 对中位差 1、均值 0.806；其余 4 对所检内容区域差异为 0。5 个全宽接缝已目视检查，原始内容连续、无中间工具栏、无重复滚动条。
- 本地证据：`artifacts/sample-analysis.json`、`artifacts/samples-stitched.png`、`artifacts/seam-*.png`，均被 Git 忽略。原始样本未修改。
- 边界：自动检测是保守规则，不能覆盖任意浮层；支持用户提供局部遮罩，界面在 S2。0 未恢复只针对已识别遮罩，不是对图像所有遮挡的保证。浏览器路径与真机容量尚未验收。

### S2 · 2026-09-09

- Astro + React 编辑器，中英文工具入口；稳定文件顺序、增删/排序/反转、拖放/粘贴处理、Web Worker 分析与合成、进度/取消、缩略预览、可编辑接缝、局部遮罩、实体色隐私遮挡、PNG/JPEG 与选择缩小导出。Web Share 按浏览器能力显示。
- 浏览器正常导入 6 张私有样本，预览/PNG 下载 1179×11444。合成输入三张 320×700，导出 320×1214，与独立已知原图逐像素相等。
- 浏览器拖动隐私遮挡后导出，内部 11484 个抽查像素全部为指定实体色；50% JPEG 下载可打开，160×608（逐段整数坐标取整）。
- 手动位移修改立即禁用旧结果导出并清除旧遮挡；重置/应用恢复。反转顺序产生 3/2/1 并清除旧预览；清空和重新导入可用。
- 无关但重复布局输入曾错误匹配，补原图纹理验证与回归；现在显示需调整且无导出。用户明确选“直接连接”后生成 320×1400。
- 补中间重复截图的接缝规划回归。`npm test` 12/12，`npm run typecheck` 与生产 `npm run build` 通过。实际图片及 UI 截图均在忽略的 artifacts 中。
- 取消行为以浏览器 DOM 按钮受控时序检查，并重新计算验证恢复；普通自动化点击在本机处理结束后才抵达，未将该点击超时算作通过。
- 边界：Web Share 系统面板、真实触屏拖动、真机内存未测。站点指南/页脚目标及部署配置在 S3 完成，当前不表示整站公开交付。

### S3 · 2026-09-09

- 完成中英文首页、指南、隐私、关于、条款与 404，共 14 个 HTML 页面。未编造运营者联系方式；发布前通过 PUBLIC_CONTACT_EMAIL / PUBLIC_SITE_URL 配置真实信息。
- Workers Static Assets 配置、Pages 部署说明、安全头、产物与本地链接检查。无 Worker 业务脚本、数据库或上传接口，广告仍未接入。
- 干净 `npm ci` 后 `npm run verify`：12 个测试、类型检查、生产构建与 14 页面链接/隐私资源隔离/资产大小检查通过。
- Wrangler 4.130.0 的 Miniflare 带入 Sharp 0.35.2，修复版本统一覆盖为现有 Sharp 0.35.4。重新生成锁文件并干净安装；`npm ls sharp` 无 invalid，`npm audit` 0 vulnerabilities。没有降级 Wrangler 或运行 audit fix --force。
- `WRANGLER_SEND_METRICS=false npm run deploy:check` 成功，33 个静态资产，未做远程发布。`wrangler dev --local --port 8788` 正常运行；安全头实读存在，不存在路径返回 404。
- 通过 Cloudflare 本地服务，从中文首页进入编辑器，导入 6 张原图并下载 1179×11444；网络观察仅有同源 CSS/JS/Worker 和本地 blob，没有图片上传。当前也没有第三方追踪/广告脚本。
- 390×844 模拟视口真实暴露了内部网格溢出，修复来源列表最小宽度与网格轨道后复测：结果右边界 373px、图片 275px、保存控件 336.38px，均在视口内。该视口完成合成图下载，与 320×1214 已知答案逐像素相等。
- 英文指南、中文隐私页通过浏览器正常打开；桌面首页及窄屏结果截图在 artifacts。所有私人测试材料均保持忽略。
- 未运行：物理 iPhone/Android 的相册和系统分享、真正触屏遮挡操作、大陆网络、多设备长期稳定性、实际 Cloudflare 部署、域名与广告审核。模拟窄屏不是手机真机验收。

## S4 的准确缺口

目前已交付可部署本地版本。继续公开发布时需指定 Cloudflare 账户/项目与域名、真实运营联系信息；广告需要实际发布站点、平台账户与审批、地区同意配置。真实手机与大陆网络验证需要对应设备/网络。没有擅自创建远程项目、修改域名、申请广告账户或发布样本。

## 后续会话入口

先读本文件，核对 Git status 与当前切片证据。选择第一个 Pending 切片开始；只有一片 In progress。每片完成必要测试和实际输出核验后，更新本文件并独立提交。遇到未授权的外部发布/账户申请时先完成本地可交付物，准确记录缺口。

### E1 · 2026-09-09

- 移除四数字范围表，改为选截图后的原图矩形编辑：框选、移动、四角缩放、单个删除、20 步撤销、恢复自动识别、放大与方向键微调。
- 修改范围先显示在原图上，应用后更新长图；清楚区分恢复范围与实际裁剪，继续使用原有真实像素覆盖逻辑。
- 新增 3 个坐标行为回归：逆向框选、移动不越界、缩放不翻转；初次模块缺失为 Red，最终 15/15 测试通过，类型/构建/静态检查通过。
- 浏览器鼠标完成框选→移动→缩放→撤销→键盘微调→应用→删除/撤销；恢复自动识别后导出与 320×1214 已知原图逐像素一致。
- 390px 模拟视口验证添加/撤销和范围编辑布局；不等于物理手机验证。证据位于忽略的 artifacts/regions-*.png。

### E2 · 2026-09-09

- 新增长图下方“裁剪首尾”：顶部/底部局部预览、可拖边界、红色待删除区、滑条、键盘微调、单边重置及恢复完整首尾。默认 0，不裁每张输入、不改中间接缝和宽度。
- 主预览即时显示裁剪结果与尺寸。原图和缩小导出使用同一边界映射规则；拒绝负数、非整数与空图片。保持已有隐私遮挡的未裁剪内容坐标，先写入真实像素再裁剪。
- 重新拼接或应用接缝/范围后重置裁剪和遮挡；中英文指南及 README 已说明。
- 新增 4 个回归：保留区逐像素相等、无效/极限裁剪、缩小边界取整、裁剪与缩小后遮挡仍覆盖原内容。初次缺少模块为 Red；最终 19/19 测试、类型/构建/14 页产物检查通过。
- 浏览器鼠标拖动裁剪边界：320×1214 → 顶部 37px / 底部 63px → 320×1114，导出逐像素等于已知原图的对应区间；50% 导出为 160×557。
- 先裁剪再绘制隐私遮挡，再调整顶部裁剪并恢复完整首尾：遮挡保持原内容位置，导出 320×1051 的对应像素全部为实体色。
- 390px 模拟视口，通过 CDP touchStart/move/end 拖动首尾边界，真实 6 图 1179×11444 裁去 184/258px 后导出 1179×11002。全部 12,971,358 个保留像素与同版未裁剪浏览器导出的对应区域相等。页面宽度 390，无横向溢出。
- UI 与导出证据保存在忽略的 artifacts/crop-*.png、artifacts/edges-*.png；仅本地。当前 8788 服务产物已更新，未刷新用户正在编辑的页面，避免清空用户当前会话。
- 未测：物理 iOS / Android 设备、系统分享面板。模拟触摸不等于真机验收。未进行外部发布。
