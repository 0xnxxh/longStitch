{
  "name": "longstitch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "samples": "node --experimental-strip-types scripts/check-samples.ts"
  },
  "devDependencies": {
    "sharp": "0.35.4"
  }
}
